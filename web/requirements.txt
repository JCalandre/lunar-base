fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.20
# Authentication: bcrypt verifies game-account passwords from lunar-tear's
# auth.db; itsdangerous signs the login session cookie (Starlette
# SessionMiddleware).
bcrypt>=4.0
itsdangerous>=2.0
# Runtime dependencies for master-data work:
#  - lz4: tools/extract_names.py decompresses lunar-tear's lz4-packed asset
#    bundles, and the Admin > Events editor de/recompresses master-data tables.
#  - pycryptodome + msgpack: the Admin > Events editor (services/masterdata_bin.py)
#    decrypts, edits, and re-encrypts the master-data bin the game server reads.
lz4>=4.0
pycryptodome>=3.20
msgpack>=1.0
