fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.20
# Runtime dependency: tools/extract_names.py (imported by the name-resolution
# services, e.g. karma_service) decompresses lunar-tear's lz4-packed asset
# bundles. pycryptodome/msgpack are NOT needed here — those are only used by the
# separate master-data dump step in setup.bat / setup.sh.
lz4>=4.0
